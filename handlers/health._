package handlers

import "net/http"

// Health handles requests for the health endpoint
type Health struct{}

func (h *Health) ServeHTTP(rw http.ResponseWriter, r *http.Request) {
	rw.WriteHeader(http.StatusOK)
}
