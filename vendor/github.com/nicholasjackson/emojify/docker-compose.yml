version: '2'
services:
  emojify:
    image: docker.io/nicholasjackson/emojify
    ports:
     - "9090:9090"
    links:
      - machinebox
    environment:
      - "FACEBOX=http://machinebox:8080"
  machinebox:
    image: machinebox/facebox
    environment:
      - MB_KEY=${MB_KEY}
